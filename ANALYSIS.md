# 원격수사 (Enkaku Sousa ~Shinjitsu e no 23-nichikan~) 분석 노트

- ISO: `D:\psp\원격수사\Enkaku Sousa Shinjitsu eno 23nichikan.iso` (739 MB)
- 게임 ID: `UCJS10088` / 개발: MEDIA.VISION
- 전작: `D:\psp\당신을용서할수없어` (Anata o Yurusanai) — 그쪽은 `f_wanpaku.pgf` 교체 방식
- 작업 스크립트: `D:\psp\원격수사\work\`, 추출물: `iso_extract\`, 압축해제: `dump\`

## 1. ISO 구성

| 파일 | 크기 | 내용 |
|---|---|---|
| `SYSDIR/BOOT.BIN` | 1.19 MB | 복호화된 실행 ELF |
| `USRDIR/0000` | 3.2 MB | 시스템/UI 텍스처 449장 + 시나리오 스크립트 바이트코드 + 소형 SGXD |
| `USRDIR/0001` | 6.5 MB | 배경·CG 252장(512×256 T8) + 타일 이미지 76장(시스템 메시지 포함) |
| `USRDIR/0002` | 8.5 MB | 캐릭터 스프라이트 1698장 (512×256, 32×32 타일) |
| `USRDIR/0003` | 7.2 MB | 배경 125장 (512×256, 32×32 타일) |
| `USRDIR/0004` | 0.8 MB | 씬/애니메이션 파라미터 (IEEE float) |
| `USRDIR/0010`,`0011`,`0013` | 39/2.5/39 MB | SGXD 사운드뱅크 (0010 == 0013, 바이트 동일) |
| `USRDIR/0012` | 404 MB | 보이스 (SGXD / VSTD 청크) |
| `USRDIR/0020`~`0023` | 각 20~32 MB | PSMF 동영상 |
| `USRDIR/MODULES/*.PRX` | - | PSMF 플레이어 모듈 |

## 2. 해독한 포맷

### 2.1 압축 — Nintendo 계열 LZ11
- 헤더 `0x11` + 24bit LE 원본 크기 (0이면 뒤 u32가 크기)
- 아카이브 안에서 스트림은 **0x800 정렬 위치 + 0x40** 에 배치
  (앞 0x40은 16바이트 해시 + 제로 패딩). `0000`만 오프셋 0에서 바로 시작.
- 구현: `work/lzss.py`

### 2.2 컨테이너 — 오프셋 테이블
압축을 풀면 선두가 u32 절대 오프셋 배열이다. `table[0]`이 곧 테이블 크기.
0이 아닌 항목만 유효 레코드. 레코드 0은 메타데이터.

### 2.3 이미지 A형 (0000 전용)
메타 레코드 = u32 섹션 오프셋 16개 + 텍스처 엔트리 배열
엔트리 8바이트: `u16 width, u16 height, u32 psm` (psm 4 = T4, 5 = T8)
이후 레코드가 (팔레트, 이미지) 쌍으로 이어진다. 픽셀은 16×8 바이트 블록 스위즐.

### 2.4 이미지 B형 (0001~0003) — 타일 이미지
```
0x00  u16 width, u16 height, u16 tile_w, u16 tile_h, u16 psm, u16 flags, u32 tile_count
0x10  타일마다: 16바이트 엔트리(u16 tile_x, u16 tile_y, ...) + tile_w*tile_h*bpp/8 픽셀
```
타일은 항상 32×32 T8(1024바이트), 스위즐됨. 빈 타일은 저장하지 않아 스프라이트가 매우 압축적이다.
크기 검산: `0x10 + count * (16 + 1024)`.

## 3. 폰트 — **찾음**

### 위치
```
ISO → /PSP_GAME/USRDIR/0000 → 오프셋 0x27E000 의 LZ11 스트림
    → 압축 해제본(0x170A23 바이트)의 0x80 ~ 0x2AC80
    → 684 타일 = 1368 글리프, 175,104 바이트
```
추출: `python work/extract_font.py --sheet font.png --raw font.bin`
결과물: `font_extract/enkaku_font.png`, `font_extract/enkaku_font.bin`,
`font_extract/script_stream.bin`(압축 해제한 스크립트+폰트 스트림 전체)

### 글리프 포맷
- **16×16 4bpp**, 픽셀값 0~15는 커버리지(안티에일리어싱)이며 색은 CLUT이 결정
- 저장 단위는 **32×16 타일 = 256바이트 = 글리프 2개**가 가로로 나란히 배치된 형태
- 한 행 16바이트: 앞 8바이트가 왼쪽 글리프의 16픽셀, 뒤 8바이트가 오른쪽 글리프
  (바이트 안에서는 상위 니블이 먼저 오는 픽셀)
- 폭이 16바이트라 PSP의 16×8 블록 스위즐은 항등 — 별도 디스위즐 불필요
- 게임은 이 타일을 **그대로 VRAM에 업로드**한다. PPSSPP 텍스처 덤프에 32×16 텍스처가
  456장 나오고 각각 글리프 2개가 들어있던 이유가 이것. 같은 픽셀에 CLUT만 다른 쌍이
  존재하는 것도 사전 키워드 색(초록/주황)을 팔레트로 바꿔 끼우기 때문.

### 글리프 순서
JIS/유니코드 순서가 아니라 **게임에서 쓰이는 순서**다. 앞부분이
`新 情 報 入 手 東 京 平 発 売 …` `白川家` `事務所` `高校` `投書` `現場` `法律` `自宅` `署` …
처럼 게임 고유 단어 순으로 나온다. 즉 스크립트가 **글리프 인덱스를 직접 참조**하며,
1368자는 이 게임 전체 스크립트의 고유 문자 집합으로 보인다(일본어 VN 통상 1200~1800자).
그래서 SJIS/UTF-8 검색으로는 대사가 잡히지 않았던 것.

### 폰트가 여기 하나뿐인 근거
- `0001`(252 palette-pairs + 76 tiled-image), `0002`(564), `0003`(125), `0004`(scene)는
  전부 컨테이너 레코드로 설명되어 미지의 블롭이 없음
- `0010`~`0013`은 SGXD 오디오, `0020`~`0023`은 PSMF
- 아카이브 빈 구간 전수 조사(`work/coverage.py`) 결과 남는 것은 제로 패딩뿐
- `BOOT.BIN` 0x99F00 부근이 탐지에 걸렸으나 확인 결과 MIPS 코드 오탐

### 부수 확인 사항
- **`sceFont` / `sceLibFont` 임포트가 없다.** BOOT.BIN이 가져오는 라이브러리는
  Kernel_Library, ModuleMgrForUser, sceSuspendForUser, sceGe_user, sceDisplay, sceCtrl,
  sceImpose, scePower, sceAudio, sceAtrac3plus, sceSasCore, sceUtility, sceUmdUser,
  sceMpeg, scePsmfPlayer, scePsmf, sceFpu, scesupPreAcc, sceHprm 뿐.
  → 전작처럼 PGF를 교체하는 방식은 **불가능**.
- ISO 전체에 `PGF0`, `f_wanpaku`, `mainfont` 시그니처 없음.
- **고정 UI 문구는 전부 사전 렌더링된 비트맵 텍스처**다. 실물 확인:
  - 설정 화면 전체 (512×256 T4) — `dump/0000` rec 558
  - 「尋問のヒント」 창 (512×256 T4) — rec 898
  - 「インストールデータをロードできませんでした。」 (512×64 T4) — rec 858
  - 「セーブが終了しました。」 (512×128 T4) — `dump/0001/0393_00579040.bin`
- **대사는 런타임 생성**이다. PPSSPP 텍스처 덤프(2869장) 분석:
  - 32×16 텍스처 456장 = **16×16 글리프 2개**가 들어간 인덱스 텍스처
  - 같은 픽셀 내용에 CLUT 해시만 다른 쌍이 존재 → 색(흰색/초록/주황 사전 키워드)은 팔레트로 처리
  - 32×32 텍스처 2388장은 대부분 배경/UI를 32×32로 올린 조각
  - 즉 게임이 글리프를 래스터화해 텍스처 캐시에 올린다 → **원본 글리프 세트가 어딘가 존재**

### 같은 스트림의 나머지 구조
```
0x000000 ~ 0x000080   헤더 (섹션 포인터 9개 + 크기 필드)
0x000080 ~ 0x02AC80   글리프 테이블 (684 타일 / 1368 글리프)   ← 폰트
0x02AC80 ~ 0x169A70   스크립트 VM 바이트코드
                      09 10 <u32> / 13 10 <u32> / 01 <u32 포인터> / 14 ... 형태
0x169A70 ~            81바이트 간격의 엔트리 포인터 테이블
```

## 3.5 대사 인코딩 — **해독 완료**

### 문자 인코딩
Shift-JIS 골격을 쓰되 한자 영역만 게임 전용 글리프 인덱스로 갈아끼운 가변 길이 인코딩이다.

| 바이트 | 의미 |
|---|---|
| `0x00`~`0x1F` | 제어 코드 |
| `0x11` | 개행 |
| `0x0F`, `0x16` | 루비/인라인 마크업 (뒤에 인자 1바이트) |
| `0x07 0x1C` … `0x12 0x10` | 텍스트 시작 / 끝 |
| `0x28`~`0x7A` | **히라가나 1바이트**. index = code − 0x28, 오십음도 순 ぁ..ん (83자) |
| `0x81 xx` | Shift-JIS 약물 그대로 (`、。【】…？！（）　`) |
| `0x82 xx`, `0x83 xx` | 실제 Shift-JIS 전각 가나 (탁음 카타카나 등) |
| `0x88`~`0x8D` + trail | **한자 2바이트**. 글리프 인덱스 = (lead − 0x88) × 256 + trail |
| `0xA1`~`0xDF` | JIS X 0201 **반각 카타카나** (게임은 전각으로 그림) |

### 검증
- `886b`→글리프 107 = 光, `886c`→108 = 志 → 최빈 화자 【光志】 3125회 (주인공)
- 화자 `[55 45 66]` = のぞみ, `[b5 da]` = オレ, `[cd d9 8370 b0]` = ヘルパー, `ドアフォン`
- 히라가나 최빈 코드 12개가 최빈 히라가나 12자(い か し の た な て っ ん で が と)와 정확히 일치
- 스크린샷 대사가 그대로 복원됨:
  `オレ、<16:35>[771][179]<0f:32>たいほ<0f:16>ぐされたんだっけ……`
  → 「オレ、〔루비 逮捕/たいほ〕されたんだっけ……」

### 규모
- 텍스트 스팬 8,822개 / 출력 줄 27,237
- 총 220,247자 (가나 155,882 + 한자 64,365)
- 실제 사용된 한자 글리프 1,334종 (폰트 1,368자 중)
- 추출 결과: `font_extract/script_text.txt`
  (`python work/decode_script.py --kata-base 0xa1 --out script_text.txt`)

### 가나 폰트 위치
한자 폰트와 별개로 **가나 폰트는 `BOOT.BIN` 안**에 있다. 0x92060 부근부터 히라가나·카타카나가
오십음도 순으로 이어진다(같은 32×16 4bpp 타일 포맷). 정확한 시작 오프셋은
`work/align_font.py`로 정렬을 잡아 확인한다.

### 루비(후리가나) 문법 — 확정
```
<베이스 문자 n개> 0F <ASCII 숫자 n> <읽기 가나…> 0F
```
숫자가 앞의 몇 글자를 가리키는지 알려준다. 닫는 `0F`는 인자가 없다.
주의: 숫자 범위 `0x31`~`0x39`는 가나 코드 お~げ와 겹치므로, 여는/닫는 판정은
바이트를 보지 말고 상태로 추적해야 한다.

스크립트 전체에서 루비 4,193회 / 서로 다른 쌍 989개 / 936개 글리프가 주석을 받는다.

## 2.5 LZ11 압축기 — 완성

`work/lz11_compress.py`. 3바이트 키 해시 체인 + lazy matching.
매치는 현재 위치와 겹칠 수 있다(디코더가 바이트 단위로 복사하므로) — 이걸 허용해야
긴 제로 구간이 제대로 줄어든다.

| 대상 | 원본 | 내 압축기 | 게임 원본 압축본 |
|---|---|---|---|
| `0004` 스트림 1 | 161,428 | **42,113** | 42,921 |
| 스크립트 스트림 | 1,509,923 | **597,494** | 605,757 |

둘 다 왕복 복원 바이트 단위 일치. **원본보다 작게 압축되므로 재삽입 시 용량 여유가 생긴다.**

## 2.6 블록 헤더 = MD5

각 블록 앞 16바이트의 정체를 규명했다.

```
블록 = [16바이트 MD5][0x30 제로][LZ11 스트림][다음 블록까지 제로 패딩]
MD5 대상 = 블록의 +0x40 부터 다음 블록 시작 직전까지 (패딩 포함)
블록 크기 = roundup(0x40 + 압축크기, 0x800)
```

동일한 스트림은 동일한 헤더를 갖는다는 관찰에서 출발했고, `BOOT.BIN` 0xE3DA4에 실제
MD5 T 테이블이 있는 것을 확인한 뒤 대상 범위를 특정했다.
(초기 상수가 안 잡히는 것은 MIPS에서 lui/ori 즉시값으로 로드되기 때문이다.)

검증: `work/stream_header.py` — `0001`은 36블록 전부 일치.
**재삽입 시 패딩까지 채운 뒤 MD5를 계산해야 한다.**

### 미해결
- `stream_header.py`의 순차 블록 워커가 일부 아카이브에서 중간에 멈춘다.
  헤더 규칙 자체는 확인됐고, 워커의 예외 처리만 보완하면 된다.

## 3.6 한자 글리프 → 유니코드 대응표

`font_extract/charmap.json` (1,368행). 생성: `python work/resolve2.py --out charmap.json`

### 방법
1. **루비 기반** — 루비는 글리프 열의 읽기를 정확히 알려준다. pykakasi의 한자 사전을
   읽기→단어로 뒤집어, 길이와 읽기가 맞는 단어를 찾고 비트맵 유사도로 후보를 고른다.
2. **부트스트랩** — 만장일치로 정해진 글리프를 고정한 뒤 재계산. 한 글리프가 정해지면
   그 글리프가 등장하는 다른 루비 주석이 함께 풀린다.
3. **비트맵 전용** — 루비가 없는 글리프는 형태 유사도만으로 매칭하되,
   폰트에 중복 글리프가 없다는 1:1 제약을 걸어 남은 문자에만 배정한다.

비트맵 유사도는 잉크 바운딩 박스로 정규화한 뒤 약한 블러를 씌워 ZNCC를 계산하고,
6종 일본어 폰트(YuGothic/MS Gothic/Meiryo/MS Mincho)에 대해 평균낸다.

4. **문맥 기반** (`work/improve_charmap.py`) — 한자 연쇄는 단어다. 같은 연쇄에 이미 아는
   글자가 하나라도 있으면 사전이 나머지를 좁힌다(「被+?」→ 被害·被疑·被告). 뒤따르는
   오쿠리가나가 사전 항목의 활용 어미와 맞으면 가점. 루비로 확정된 807자를 앵커로 고정하고
   새로 정해진 글자를 다시 앵커로 삼아 반복한다.

### 게임 파일에 매핑 테이블은 없다 (확인함)
`work/find_mapping.py`로 BOOT.BIN·전체 압축 해제 스트림·원본 아카이브에서
글리프→SJIS 배열과 글리프→유니코드 u16 배열을 전수 검색했으나 전부 오탐이었다
(BOOT.BIN 0x10D19A의 165개 연속 히트는 MIPS 명령어 배열, dump 쪽은 텍스처 픽셀).

없는 것이 당연하다. `work/check_order.py`로 확인한 결과 스크립트에서 각 글리프가
**처음 등장하는 순서**가 인덱스 순서와 93.4% 일치하고 평균 이탈이 32.5다.
즉 폰트는 서브셋 툴이 스크립트를 훑으며 새 글자마다 다음 인덱스를 부여해 만든 것이고,
**한자 코드가 곧 글리프 인덱스**라 런타임은 대응표가 필요 없다.
(이 규칙은 재삽입 때 새 폰트 인덱스를 배정하는 데 그대로 쓸 수 있다.)

5. **오쿠리가나** (`work/okurigana_pass.py`) — 사전이 활용형을 통째로 키로 갖는다
   (`戻ら`/`戻り`/`戻る`/`戻れ`/`戻ろ`). 어미 하나로는 후보가 수백이지만, 같은 글리프가
   여러 활용형으로 등장하면 교집합이 좁아진다. 관찰된 모든 어미에 지지도를 누적하고
   사전 누락에 대비해 미스에는 감점만 준다.
6. **글자 단위 읽기** (`work/reading_pass.py`) — 3번의 루비 패스는 주석된 단어 전체가
   사전에 있을 것을 요구해서 인명·고유명사를 통째로 버렸다. 여기서는 읽기를 글자 단위로
   맞춘다. 여러 글리프에 걸친 읽기는 가능한 모든 분할을 시도하고, 이미 확정된 글자는
   고정해 분할을 검증한다.

### 정확도 (정직하게)

글리프 수보다 **등장 횟수 비중**이 실제 체감에 가깝다. 미해결로 남은 글자는 대부분 희귀자라
글리프 수 기준으로는 커 보여도 실제로 마주칠 일은 적다.

| 근거 | 글리프 | 등장 횟수 | 비중 |
|---|---|---|---|
| 루비 (만장일치) | 353 | 31,960 | 46.0% |
| 루비 (그 외) | 454 | 20,848 | 30.0% |
| 문맥 | 225 | 10,496 | 15.1% |
| 오쿠리가나 | 66 | 3,648 | 5.2% |
| 글자 단위 읽기 | 39 | 722 | 1.0% |
| 오쿠리가나 (약함) | 21 | 175 | 0.3% |
| **비트맵 추측** | **210** | **1,619** | **2.3%** |

**한자 등장 69,488회 중 97.6%가 근거 기반이다.** 남은 210자는 루비도 문맥도 없는 희귀자로,
이 접근으로는 수렴했다(문맥 패스 2라운드째 신규 0). 더 줄이려면 육안 확정이 필요하다.
`charmap_final.tsv`의 `uses` 열로 정렬하면 영향 큰 것부터 처리할 수 있다.

2자 이상 한자열 21,878개 중 사전 단어로 시작하는 비율:

| 단계 | 적중률 |
|---|---|
| 순수 비트맵 매칭 | 정답 14자 중 6자 (43%) |
| + 루비 (`charmap.json`) | **60.5%** |
| + 문맥 (`charmap2.json`) | **76.7%** |

**low 등급은 신뢰하지 말 것.** 남은 오류 예: `太派`(→立派), `虞據`(→貫禄), `白側家`(→白川家).
문맥 단계에서 `表軋`→`表札`, `褂`→`掛` 처럼 교정된 것도 있다.

### 산출물
| 파일 | 내용 |
|---|---|
| `font_extract/charmap_final.json` / `.tsv` | **최종 대응표** (문자·근거·신뢰도·등장횟수·대안) |
| `font_extract/script_full_raw.tsv` | 한자를 `[인덱스]`로 둔 전체 스크립트 — **100% 정확** |
| `font_extract/script_full_ja.tsv` | 대응표를 적용한 일본어 스크립트 |
| `font_extract/ruby_pairs.json` | 루비 (글리프열, 읽기) 쌍 |

`charmap.json`~`charmap6.json`은 단계별 중간 산출물이다. 최종본은 `charmap_final.*`.

## 3.7 대사 전체 추출

`work/extract_all_text.py` — 마커에 의존하지 않고 텍스트 토큰의 최대 연쇄를 모두 수집한다.

처음에는 `07 1C` 마커로 스팬을 잡았는데 텍스트의 9%를 놓쳤다. 엔진이 `07 81`, `17` 등
여러 마커로 텍스트를 열기 때문이다. 토큰 기반으로 바꾸자 한자 참조가 64,365 → 69,488로
늘었다(스트림 전체 한자 70,887 대비 98.0%).

바이트코드 피연산자가 히라가나처럼 보일 수 있으므로, 연쇄는 길이가 3토큰 이상이고
2바이트 토큰(한자/약물)을 최소 1개 포함할 때만 채택한다. 바이트코드는 그 조건을 만족하지 못한다.

- 대사 9,626개 / 게임 내 표시 줄 29,111줄
- 한자 69,462자 + 가나 169,702자
- 화자 이름 태그가 붙은 행 8,875개
- TSV 3열(`offset` / `lines` / `text`). 게임 내 줄바꿈은 `\n` 리터럴로 이스케이프

### 정확도를 더 올리려면
1. 루비가 없는 561자 — 오쿠리가나 문맥을 사전과 대조하면 상당수 확정 가능
2. 최종 검수는 글리프 이미지를 직접 띄워 눈으로 확인 (`work/showglyph.py`)
3. 번역 작업 자체는 `script_text.txt` + 글리프 렌더링을 기준으로 하는 편이 안전하다

## 3.8 실기 검증에서 잡은 두 버그

둘 다 실제 게임 화면과 대조하기 전에는 드러나지 않았다.

### 니블 순서
PSP의 4bpp(T4)는 **하위 니블이 왼쪽 픽셀**인데 `font.py`만 상위 니블을 먼저 놓고 있었다.
`texpack.py`는 올바랐으므로 배경·UI 텍스처는 정상이었고, 한자는 획이 굵어 좌우 1픽셀이
바뀌어도 형태가 살아남아 눈치채지 못했다. 한글은 획이 얇아 바로 드러났다.

PPSSPP가 업로드한 타일과 대조해 확정: 니블을 바로잡자 상관계수 +0.697 → **+0.969**.
손대지 않은 원본 절반(대조군)도 같이 올라간 것이 결정적 근거였다.

### 글리프 인덱스 공식
```
글리프 인덱스 = (리드 - 0x88) * 253 + 트레일     ← 정답
```
`* 256`이 아니다. 리드 `0x88`(첫 블록)에서는 두 공식이 같은 값을 내므로 슬롯 107·108·200은
정상 동작하고 400 이상만 어긋났다. 각 블록의 253·254·255가 "안 쓰이는 슬롯"으로 보였던 것도
잘못된 공식이 만든 허상이었다 — 실제 테이블은 빈틈 없이 연속이다.

화면에 나온 한자 `割否検騒罰呵宿架託`가 ×253으로 계산한 글리프와 정확히 일치해 확정했다.

두 버그 모두 대응표 생성에 직결되어 있었다. 바로잡고 재생성한 결과:

| 지표 | 수정 전 | 수정 후 |
|---|---|---|
| 사전 단어 적중률 | 77.4% | **89.7%** |
| 근거 기반 등장 비중 | 97.6% | **99.1%** |
| 비트맵 추측 | 210자 / 2,341회 | **170자 / 629회** |

## 3.9 바이트 예산 — 재삽입의 핵심 제약

스크립트 바이트코드가 텍스트를 **절대 오프셋**으로 참조하므로, 각 줄은 원본이 차지하던
바이트 수를 넘을 수 없다.

실측 예 (`0x13EBB8`):
```
일본어 「【光志】/なんでオレ、/こんなとこにいるんだ……？」   33 바이트
한국어 「【광지】/어째서 내가/이런 곳에 있는 거지……?」      50 바이트
```
일본어는 가나가 1바이트인데 한글은 2바이트라 **구조적으로 1.5배가량 넘친다.**

해결 경로는 둘이다.

1. **1바이트 코드 영역 활용** — 히라가나 `0x28`~`0x7A`(83칸)와 반각 카타카나 `0xA1`~`0xDF`
   (63칸), 합쳐 146칸이 1바이트다. 최빈 한글 음절을 여기 넣으면 평균 바이트가 크게 준다.
   단 이 글리프는 아카이브가 아니라 **`BOOT.BIN` 안의 가나 폰트**에서 오므로 그쪽도
   패치해야 한다. 대략 0x92060 부근이나 정확한 시작 인덱스는 미확정 —
   자동 매칭이 변별력을 내지 못했고, 틀리면 모든 1바이트 문자가 깨지므로 실기 확인이 필요하다.
2. **텍스트 재배치 + 포인터 재계산** — 바이트코드의 `01 <u32>` 포인터를 모두 찾아 갱신하면
   길이 제약이 사라진다. 압축 결과가 원본보다 작아 공간 여유는 있다. 근본 해결책이지만
   포인터를 하나라도 놓치면 크래시하므로 신중한 구현이 필요하다.

## 3.10 재배치(경로 B) 설계 — 조사 완료

번역문(7,841행)을 자르지 않고 넣으려면 인라인 텍스트를 늘려야 하고, 그러면 뒤쪽
바이트코드가 밀린다. 밀림에 영향받는 참조를 전수 조사한 결과는 다음과 같다.

| 요소 | 수량 | 성질 | 처리 |
|---|---|---|---|
| `0e <s32>` | 11,412 | **상대 점프** (-52 ~ 25616, 100% 유효) | 삽입 지점이 점프 구간을 가로지를 때만 델타 보정 |
| `01 <u32>` | 32,221개 사용 / **고유 주소 2,149** | **절대 주소** | 전부 재매핑 |
| 말단 엔트리 테이블 (0x169A70~) | 0x51 간격 | 절대 주소 | 재매핑 |
| 선두 섹션 포인터 | 9개 | 절대 주소 | 재매핑 |

`01`이 주소라는 근거: 명령어 경계 후보 484개 중 **478개(98.8%)가 경계에 착지**한다.
경계 밀도가 26%이므로 우연이라면 126개여야 한다. 반대로 값의 절반 이상이 소수의 상수
(`0x2afac`가 8,274회 등)로 반복되므로, 상수와 주소가 **같은 오프코드에 섞여 있다**.
따라서 값만 보고 일괄 치환하면 안 되고, 명령어 위치로 찾아 그 자리에서 갱신해야 한다.

### 시도했고 실패한 접근 (반복하지 말 것)

세 가지 모두 같은 뿌리에서 실패했다 — **명령어 경계를 확정하지 못하면 모든 바이트 패턴
분석이 피연산자 데이터에 오염된다.**

1. **디스패치 테이블 추출** (`find_dispatch.py`, `find_jumptable.py`, `find_struct_table.py`)
   - MIPS 점프 테이블 관용구 56곳 → 전부 4~15개짜리 컴파일러 switch
   - 최대 210개 포인터 배열 → 인접 데이터가 `C-UTF-8` 문자열과 log2 룩업, VM과 무관
   - 스트라이드 8~24 구조체 배열 79개 → 관찰된 길이와 맞는 길이 필드 없음
   - **결론: 길이 테이블을 동반한 디스패치 테이블은 존재하지 않는다.**

2. **제약 충족 역산** (`solve_lengths.py`, `solve_lengths2.py`)
   - 앵커 20,857개(텍스트 런 시작·끝 + `01` 타깃)로 길이표를 역산
   - 주행률 79.2%에서 정체, 위반이 5 → 31로 **증가**
   - `ff:1`, `d1:8`, `ac:3`, `da:3` 등 오프코드일 수 없는 값에 길이 배정
   - **결론: 워커가 동기를 잃은 채 노이즈에 적합했다. 이 표는 신뢰 불가.**

3. **트램펄린 (상대 점프로 확장 텍스트 우회)** (`jump_convention.py`)
   - `0e <s32>`가 무조건 상대 점프라는 전제로 설계
   - 두 규약(명령어 끝 기준 / 시작 기준) 모두 **알려진 경계 적중률 2.3%**
   - 타깃의 40~55%가 **텍스트 내부**에 착지 — 점프 타깃일 수 없는 위치
   - **결론: `0e` 사이트 대부분이 오프코드가 아니라 피연산자 바이트. 전제가 틀렸다.**

### 확정: 텍스트 블록 프레임 (`work/text_blocks.py`)

```
07 1C  <텍스트 바이트>  12 10
```

`07 1C`는 스트림에 **8,852회 등장하며 단 하나도 텍스트 내부에 없다.** 오탐 0건이므로
텍스트 블록의 경계는 100% 확정된다. 종료 마커 탐색은 반드시 **다음 시작 마커로 상한**을
걸어야 한다 — 걸지 않으면 종료 형태가 드문 블록이 뒤따르는 블록들을 삼켜 겹침이 생긴다
(실제로 간격 −1047이 나왔다).

결과: **텍스트 블록 8,822개**, 텍스트 총 432,631바이트, 블록 간 간격 최소 14 / 중앙값 49.

이것이 앞선 솔버들이 실패한 진짜 이유를 설명한다. 기존 텍스트 탐지는 "가나 범위 바이트"를
찾는 방식이라 **피연산자 데이터의 우연한 일치를 텍스트로 오인**했고, 그때마다 워커가 진짜
명령어를 건너뛰어 탈선했다. 마커 기반으로 바꾸자 위반이 0으로 떨어졌다.

### 3차 솔버도 수렴 실패 — 원인은 앵커 밀도
마커 앵커(17,644개)로 다시 풀었으나 주행률 75%에서 정체하고, 미해결 키가 56 → 209로
**증가**했다. 위반은 0이지만 이는 제약이 약하다는 뜻일 뿐이다 — 블록 간 간격이 중앙값
49바이트라 그 안에서는 워커가 자유롭게 배회해도 경계를 넘지 않는다.
`dd`, `6a`, `3c`, `b9`, `e2` 등 오프코드일 수 없는 값에 길이가 붙는 현상이 여전하다.

### 4차 시도: CSP (`work/solve_lengths_csp.py`) — 근본적 한계 확인
간격 8,408개를 제약으로 두고 전진·후진 도달가능성으로 도메인을 좁혔다. 2라운드 만에
고정점에 도달했으나 **확정된 키는 시드로 준 4개뿐**이고 572개가 도메인 10~12로 남았다.

원인은 알고리즘이 아니라 **모델 자체**다. 간격 안에 나타나는 서로 다른 키가 **576개**인데,
실제 ISA의 오프코드가 576종일 리 없다. 즉 모델이 **오프코드와 피연산자 바이트를 구분하지
못한다.** 자유 변수 576개에 "합이 N" 형태의 약한 제약 8,408개로는 과결정이 아니라
심한 **부족결정**이며, 도달가능성 전파가 거의 아무것도 쳐내지 못한다.

**결론: 통계·제약 기반 접근으로는 이 길이표를 복원할 수 없다.** 어떤 바이트가 오프코드인지를
알아야 제약이 의미를 갖는데, 그 정보가 스트림 안에 없다. 인터프리터 코드를 읽는 것 외의
경로는 없다.

## 3.11 인터프리터 코드에서 직접 읽어낸 것 (MIPS)

통계가 아니라 실제 코드를 읽어 확정한 사실들. `work/mips.py`, `work/disasm.py`,
`work/find_interpreter.py` 사용.

### 토큰 디코더 `0x0884BE84`
인자: `$a0`=포인터, `$a1`=출력 길이(int), `$a2`=출력 코드(halfword)

```
길이 = 2   if  b0 ∈ [0x81,0x9F] 또는 [0xE0,0xFC]
길이 = 1   그 외
```

**히라가나 매핑이 코드로 확증됨:**
```
slti $v1, $a0, 40        ; b < 0x28 이면 통과
slti $at, $a0, 123       ; b >= 0x7B 이면 통과
addiu $v0, $a0, 119      ; b + 0x77
ori   $v0, $v0, -32256   ; | 0x8200
```
즉 단일 바이트 `0x28`~`0x7A` → SJIS `0x8200 | (b + 0x77)`.
검산: `0x28` → `0x829F` = **ぁ**. 빈도 분석으로 역산했던 매핑과 완전히 일치한다.

반각 카타카나 `0xA7`~`0xDD`는 `0x088FA394` 부근의 2바이트 테이블로 전각 변환된다.

### 텍스트 제어 코드의 피연산자 크기 `0x0884CAC4`
이 루틴은 텍스트를 훑으며 제어 코드를 처리한다. 분기를 따라가면 코드별로 포인터(`$s4`)를
얼마나 더 전진시키는지가 나온다 — **이것이 피연산자 크기다.**

| 코드 | 추가 소비 바이트 |
|---|---|
| `0x13` | 1 |
| `0x16` | 1 |
| `0x18`, `0x19` | 2 |
| `0x1B` | 2 |
| `0x09` | 4 |
| `0x1E` | 4 |
| `0x1F` | 6 |
| `0x03`,`0x04`,`0x05`,`0x07` | 3 또는 1 (조건부) |

`0x03/04/05/07` 계열은 `0x0884CD78`부터 세 바이트를 읽어 각각 `-0x30`을 빼고
`×100 + ×10 + ×1`로 합산한다. **ASCII 숫자 3자리 파싱**이며, 루비 태그
`0F <ASCII 숫자>`가 왜 숫자를 문자로 싣는지가 여기서 설명된다.

### `0x0884CEF8`의 정체와 호출 그래프 (막다른 길)
`0x0884CEF8`은 분기 목적지가 아니라 **함수 에필로그**다. 따라서 코드
`0x0E,0x10,0x11,0x12,0x13,0x14,0x15,0x1C`는 텍스트 측정 루프를 **종료**시키는 코드다.
루프 자체는 `$s3 < 256`(문자 수 상한)으로 돈다.

호출자를 거슬러 올라가는 시도는 **발산했다**:
- `0x0884CAC4`(측정) ← 호출 지점 10곳 / 함수 8개
- `0x0884CF1C`(레이아웃) ← 호출 지점 **62곳 / 함수 18개**

호출자들이 여러 UI 화면 코드에 넓게 퍼져 있다. 즉 이 둘은 **범용 텍스트 유틸리티**이며
스크립트 VM 전용이 아니다. 호출 그래프 상향 추적으로는 인터프리터에 닿을 수 없다.

### LZ11 압축 해제 루틴 확정: `0x0886E79C` (`work/find_decompressor.py`)
포맷 상수 `0x11`과 `0x111`을 모두 가진 유일한 소형 함수(135명령). 코드로 확인한 근거:

```
andi $a0, $t0, 240 ; addiu $v1, $zero, 16 ; beq    ; 상위 니블 == 0x1
andi $a0, $t0, 15  ; addiu $v1, $zero, 1  ; beq    ; 하위 니블 == 1 → 타입 0x11
sb $t3, 0($t1) ; addiu $t1, $t1, 1                 ; 리터럴 출력, $t1 = 출력 포인터
sra $t3, $t5, 4 ; addiu $t6, $t3, 3                ; 매치 길이 계산
```

시그니처: `decompress(src, size, dst)` — `$a2`가 출력 버퍼.

**호출자가 단 3곳으로 수렴한다** (텍스트 유틸리티는 62곳으로 발산했던 것과 대조):

| 호출 지점 | 소속 함수 |
|---|---|
| `0x08843140` | `0x08842FC4` |
| `0x08843430` | `0x0884324C` |
| `0x0886D6A0` | `0x0886D5D4` |

### 실행 파일에 디버그 심볼이 남아 있다 (중요)
할당자 호출이 `alloc(size, align, __FILE__, __LINE__)` 형태이고 **소스 파일명 문자열이
그대로 들어 있다.** 세 호출자의 정체가 이것으로 확정된다:

| 호출 함수 | 소스 위치 |
|---|---|
| `0x08842FC4` | **`read.c:464`** |
| `0x0884324C` | **`read.c:632`** |
| `0x0886D5D4` | `file_access.c:162` |

공통 구조:
```
jal 0x0886E768          ; LZ11 헤더에서 원본 크기 취득
addiu $a1, $zero, 16    ; 정렬
addiu $a2, <__FILE__>   ; 파일명 문자열
jal 0x088467C0          ; alloc(size, 16, file, line)
addiu $a3, <__LINE__>
jal 0x0886E79C          ; decompress(src, size, dst=$v0)
```

`read.c`가 리소스 로더다. 스크립트 버퍼는 `read.c:464` 또는 `read.c:632`에서 할당된다.

### 모듈 지도 (`work/find_source_names.py`)
소스 파일명 **49개**를 추출했다. 참조 주소가 모듈 배치 순서를 알려주므로 바이너리를
이름 있는 구역으로 나눌 수 있다. 주요 구간:

```
filesys/filedev/filectxt/fileslot/filereq/fileutil/filepsp   0x0881A308~0x08822EAC
heapsys/heapslot                                             0x08825FB0~0x08826614
sgxsys/sgxbmg/sgxres/sgxsnd/sgxseq/sgxconf/sgxpsp (사운드)     0x08829D80~0x0883CE74
read.c                                                       0x08843128, 0x08843418
snd.c                                                        0x088466A0
objctrl.c                                                    0x088468BC~0x088477CC
  ← 이름 없는 구간 0x08847800~0x0884FDEC →
movie.c / moviesound.c / movievideo.c                        0x0884FDEC~0x08850A28
evtuty.c                                                     0x08855A78
list.c / filterctrl.c / wipectrl.c                           0x0885B7B4~0x0885DDF4
scene.c 계열 + model/mesh/motion                              0x08863704~0x088695D8
file_access.c                                                0x0886D650~
mu_config.c / media_install.c / mu_quest.c                   0x0886F954~0x088745C4
```

**텍스트·스크립트 코드는 `objctrl.c`와 `movie.c` 사이의 이름 없는 구간에 있다.**
지금까지 찾은 후보가 모두 여기 들어간다: `0x0884A030`, `0x0884B680`,
`0x0884CAC4`(텍스트 측정), `0x0884CF1C`(레이아웃), `0x0884E3DC`.
이 모듈은 할당자에 `__FILE__`을 넘기지 않아 이름이 남지 않았다.

`evtuty.c`(이벤트 유틸리티, 0x08855A78)가 이름상 스크립트와 가장 가깝지만 참조가 1곳뿐이다.

**이 디버그 문자열은 앞으로의 역분석 전반에 쓸 수 있다.** 다른 함수들도 할당자나 로그
호출에 파일명을 넘긴다면 모듈 단위로 코드를 식별할 수 있어, 인터프리터를 찾는 데도
`script.c` 같은 이름을 직접 검색하는 접근이 가능하다.

이 중 하나가 아카이브 `0000`의 스트림을 스크립트 버퍼로 풀어낸다. 그 버퍼 포인터가
저장되는 전역을 찾고, 그 전역을 읽는 코드를 역추적하면 인터프리터에 도달한다.
스크립트의 `01 <u32>`가 파일 상대 오프셋이므로 인터프리터는 반드시
`버퍼 베이스 + 오프셋` 연산을 하며, 이것이 확인 지표가 된다.

### (이전 방향 — 참고)
스크립트 버퍼를 읽는 코드를 직접 찾아야 한다. 스크립트의 `01 <u32>` 절대 주소는
**파일 상대 오프셋**이므로, 인터프리터는 반드시 `버퍼 베이스 + 오프셋` 연산을 한다.
아카이브 `0000`의 스트림이 메모리에 올라가는 지점을 찾고, 그 버퍼 포인터를 사용하는
코드를 역추적하는 편이 호출 그래프 추적보다 수렴 가능성이 높다.

주의: 아래 표는 **텍스트 내부** 제어 코드 표다. 텍스트 블록 사이의 스크립트 레벨 명령
길이는 별도 루틴에 있으며 아직 찾지 못했다. 다만 `0x0884CB7C` 부근에서 코드
`0x0E,0x10,0x11,0x12,0x13,0x14,0x15,0x1C`가 공통 지점(`0x0884CEF8`)으로 분기하는
경로가 따로 있어, 모드에 따라 처리가 갈리는 구조로 보인다.

## 3.12 인터프리터 확정과 길이표 (검증됨)

**인터프리터: `0x0884A030`** (`work/extract_lengths2.py`)

스크립트 포인터는 컨텍스트 구조체의 **`+17016`(0x4278)** 에 있다. 각 핸들러가 끝날 때
자기 명령 길이만큼 전진시킨다:
```
lw    $v0, 17016($s4)
addiu $v0, $v0, N        ; N = 그 오프코드의 명령 길이
sw    $v0, 17016($s4)
```
오프코드 분기는 `addiu $v0, $zero, <코드>` + `beq/beql $s0, $v0` 체인이다.

### 코드에서 직접 뽑은 길이표
| 오프코드 | 길이 | 오프코드 | 길이 |
|---|---|---|---|
| `0x0C` | 1 | `0x18` | 3 |
| `0x0F` | 2 | `0x19` | 3 |
| `0x10` | 1 | `0x1A` | 1 |
| `0x11` | 1 | `0x1B` | 3 |
| `0x12` | 1 | `0x1C` | 1 |
| `0x16` | 2 | `0x1E` | 5 |

### 교차 검증 통과
`0x0884CAC4`에서 독립적으로 뽑은 피연산자 크기와 완전히 일치한다(피연산자+1 = 길이):

| 오프코드 | 0x0884CAC4 | 0x0884A030 |
|---|---|---|
| `0x16` | 1+1=2 | 2 ✓ |
| `0x18`,`0x19` | 2+1=3 | 3 ✓ |
| `0x1B` | 2+1=3 | 3 ✓ |
| `0x1E` | 4+1=5 | 5 ✓ |

`0x0F`→2는 경험적으로 찾은 루비 문법 `0F <ASCII 숫자>`와도 일치한다.
서로 다른 함수·서로 다른 방법의 결과가 모두 맞아떨어졌다.

### 종료 코드
`0x00, 0x13, 0x14, 0x15`는 공통 핸들러 `0x0884A7C8`로 분기하며 **포인터를 전진시키지
않는다** — 루프 종료 코드이므로 길이가 없는 것이 정상이다.

### 완성된 디코딩 규칙
메인 루프는 **`0x0884A9A4`** 이고, 실행기를 둘 호출한다.

- **`0x0884A000`** — 기본 경로. 스크립트 포인터를 **토큰 길이만큼** 전진시킨다.
  ```
  lw   $v1, 17016($a0)
  addu $v1, $v1, $t0     ; $t0 = 토큰 디코더가 반환한 길이 (lw $t0, 24($sp))
  sw   $v1, 17016($a0)
  ```
- **`0x0884A030`** — 명령 경로. 위 표대로 절대 길이를 설정한다.

메인 루프는 텍스트와 **같은 토큰 디코더 `0x0884BE84`** 를 쓴다. 따라서:

```
길이 = 명령 핸들러가 있으면  → 표의 값
       없으면               → 1, 단 리드가 0x81~0x9F 또는 0xE0~0xFC 이면 2
```

**이것으로 스트림의 모든 바이트가 결정적으로 해석된다.** `0x01,0x02,0x03,0x07,0x09,
0x0E,0x1F`에 핸들러가 없는 것은 누락이 아니라, 기본 경로로 1바이트씩 소비된다는 뜻이다.
(따라서 통계 분석 시절의 `0x01`→5, `0x0E`→5는 틀린 값이었다.)

규칙은 `work/opcodes.py` 한 곳에 있다. 루비 `0x0F`만 상태 의존이다 —
`0F <숫자> <읽기> 0F` 에서 여는 쪽은 숫자 오퍼랜드가 있어 2바이트, 닫는 쪽은 1바이트라
표 한 칸으로는 표현할 수 없다.

### 완주 검증 (통과)
`work/walk_stream.py`. 세 시험을 두는 이유는 앞의 둘이 블록마다 **재동기화**를 하기
때문이다. 가끔 어긋나는 규칙도 거기서는 높은 점수가 나온다. 판정은 셋째가 한다.

| 시험 | 결과 |
|---|---|
| 텍스트 층 (블록 시작 → `12 10`) | 8,822 / 8,822 |
| 명령 층 (블록 끝 → 다음 `07 1C`) | 8,821 / 8,821 |
| **연속 주행 (재동기화 없음)** | **26,466 / 26,466** |

연속 주행은 `0x038B78`~`0x16E9BB`, **1,269,315바이트를 1,102,899개 명령으로** 한 번도
다시 맞추지 않고 걷는다. 길이가 한 곳만 틀려도 그 뒤 전 구간이 그만큼 밀려 이후
검문소가 모조리 빗나가므로, 100%는 우연히 나오지 않는다.

### 부수적으로 잡힌 버그: `12 10` 오탐
검증 중 텍스트 층에서 4건이 어긋났는데, **틀린 쪽은 워커가 아니라 블록 파인더**였다.
`0x1E`는 5바이트라 `1e 00 13 12 10` 형태가 나오고, 파인더의 단순 바이트쌍 검색이 그
오퍼랜드 꼬리를 닫는 마커로 착각했다. 워커는 5스텝 뒤의 진짜 `12 10`에 정확히 닿았고,
파인더가 가리킨 위치는 애초에 명령 경계가 아니었다.

`text_blocks.find_blocks()`가 이제 후보 `12 10`을 명령 경계인지 검사해 거른다.
해당 블록 4개는 이전까지 텍스트 끝이 잘못 잡혀 있었다.

### 스트림 레이아웃 (측정으로 확정)
낡은 상수 `TEXT_START = 0x2AC80`은 텍스트 시작이 아니라 **폰트의 끝**이었다.

| 구간 | 범위 | 내용 |
|---|---|---|
| 헤더 | `0x000000`–`0x000080` | u32 포인터 11개 |
| 폰트 | `0x000080`–`0x02AC80` | 175,104바이트, `enkaku_font.bin`과 **바이트 단위 완전 일치** |
| 스크립트 | `0x02AC80`–`0x170A23` | 731,922개 명령 |
| 레코드 런 | `0x16E9BF`–`0x16EB4A` | `01 <u32>` 79개 (스크립트 안에 박혀 있음) |

첫 `07 1C` 마커는 `0x038B78`이라, **텍스트 블록이 나오기 전에 56KB의 명령**이 있다.
이전 워크는 그 구간을 통째로 빠뜨리고 있었다.

### 추가된 두 명령 형태
`0x10 <u32>` = **5바이트 리터럴 푸시** (86,962개). 표에 `0x10`→1로 넣어둔 게 틀렸다.
`0x01 <u32>` = **5바이트 절대 참조** (26,526개).

닫는 마커도 다시 봐야 했다. `12 10 ff ff ff ff`는 2바이트 마커가 아니라
`12` + `10 <u32>` **한 덩어리 6바이트**다. 따라서 `block.end = close + 2`는 애초에 명령
경계가 아니었고, `0x10`=5로 고쳤을 때 검문소가 26,466→17,644로 떨어진 것은 파싱 실패가
아니라 **그 정의가 틀렸다는 증거**였다. marker와 text_end는 8,822/8,822로 유지된다.

### 참조 수집 결과: 26,537개
`work/ref_manifest.py` → `build/ref_manifest.json`

**워크로는 이 문제를 풀 수 없다는 점이 중요하다.** `01`을 1바이트로 읽든 5바이트로 읽든
검문소는 26,466/26,466으로 똑같이 통과한다 — 오독이 몇 바이트 뒤에 다시 합류하고 다음
검문소는 수천 바이트 뒤이기 때문이다. "워크가 통과했다"를 증거로 쓸 뻔했다.

실제로 판정한 것은 워크와 무관한 두 가지다.

1. **대상 바이트 편향** — 참조가 가리키는 곳의 첫 바이트:

   | 바이트 | 대상 중 | 스트림 중 | 배율 |
   |---|---|---|---|
   | `09` | 46.9% | 1.3% | 35.3× |
   | `0c` | 26.8% | 0.5% | 53.8× |
   | `0f` | 8.5% | 0.9% | 9.4× |

   스트림 자체 분포 대비 **log10 우도비 35,868**. 임의의 정수는 이렇게 못 한다.

2. **경계 착지** — 26,421/26,526 = 99.60%, 경계 밀도 54.7% 대비.

**놓친 포인터가 없다는 근거**: 같은 5바이트 모양인 `10 <u32>` 리터럴 86,962개 중
스크립트 범위에 드는 것은 **17개(0.0%)** 뿐이고, 그 17개의 대상 바이트는 `81`이 76%로
라벨이 아니라 텍스트를 가리킨다. 리터럴은 주소가 아니다.

또 하나: tail의 `01 <u32>` 79개 레코드 표는 stride 검출이라는 **완전히 다른 방법**으로
먼저 찾았는데, 워크가 79/79를 독립적으로 짚었다.

해결되지 않는 `01` 1,455개(5.2%)는 오류가 아니다. `13 10 49 00 00 00 | 01 | 10 ff ff ff ff`
처럼 `01`이 1바이트 연산으로 쓰이고 뒤에 리터럴이 오는 자리다. 주소가 아니므로
**리플로우가 건드리지 않는 게 맞다.**

### 리플로우 엔진 (`work/reflow.py`)
오프셋 맵을 breakpoint 목록이 아니라 **전체 배열**(12MB)로 잡았다. 이동한 블록 *내부*를
가리키는 참조가 27개 있는데, breakpoint 탐색은 이를 특수 처리해야 하고 조용히 틀리기 쉽다.

| 시험 | 결과 |
|---|---|
| 항등 (확장 0) | 1,509,923바이트 재구축, 참조 26,537개 재작성, **원본과 바이트 일치** |
| 재파싱 (+50%) | 마커 8,822/8,822, 텍스트끝 8,822/8,822, 참조 26,537/26,537 해결 |

경계 착지율 99.60%로 원본과 동일. 항등 시험은 번역문 없이도 돌아가고 맵 off-by-one·참조
오기입·블록 경계 오독을 잡는다. 자기 완결적이기만 한 맵은 항등은 통과하고 재파싱에서 걸린다.

### ISO 재배치 (`work/patch_iso_grow.py`)
ISO에 **LBA 15284부터 38,604섹터(79MB)** 의 빈 공간이 있다. 38,604섹터 전체가 0인 것을
확인했다(추정 아님). 이미지 *안쪽* 공백이라 ISO 크기도 PVD 볼륨 크기도 그대로다 —
바뀌는 건 해당 파일의 디렉터리 레코드 하나뿐.

주의점 둘: ISO9660은 extent와 size를 **LE·BE 두 번** 저장하므로 한쪽만 고치면 BE를 읽는
리더에서 무작위 손상처럼 보인다. 그리고 옛 extent는 그대로 둔다 — 지우는 게 깔끔해 보이지만
다른 레코드가 공유하는 데이터를 건드릴 위험이 있고, 죽은 섹터는 비용이 없다.

전 구간 검증 통과(재배치 후 레코드 경유로 바이트 일치, 다른 파일과 겹침 없음).

### 아카이브 확장 여부: 아직 미확정
`0000`의 꼬리 3,011바이트는 TOC가 **아니다** — u32 중 0x800 정렬된 값이 0개이고 상승도
반반인 고엔트로피 데이터다. BOOT.BIN에도 레이아웃 오프셋이 없다(MIPS 즉치는 16비트라
`lui`/`ori`로 쪼개지므로 32비트 리터럴 검색으로는 원래 못 찾는다). 아카이브 내
`0x27E000` 2건은 SGXD 오디오 구간의 우연이다.

**하지만 확장이 필요한지 자체가 아직 미측정이다.** 압축 크기를 재려면 한국어를 인코딩해야
하는데 `korean_slots.json`에 **항목이 5개**뿐이라 7,841행 중 7,828행이 인코딩에 실패한다.
폰트가 선행 조건이었다.

### 음절 예산 (`work/syllable_budget.py`) — 여유 있음
| 항목 | 수 |
|---|---|
| 서로 다른 한글 음절 | 973 |
| 비한글 문자 | 193 |
| **필요 글리프** | **1,166** |
| 폰트 용량 (684타일 × 2) | 1,368 |

**202칸 여유.** 상위 500음절이 전체 출현의 98.59%, 1,000음절이 100%를 덮는다.
번역 전체가 폰트에 들어간다.

### 한글 폰트 1,155자 (`work/build_korean_font.py`)
서울한강체 B, 16×16, 4× 슈퍼샘플링. 1,166자 중 문장부호 11자는 엔진이 자체 SJIS 블록에서
그리므로 제외 → **1,155 글리프를 슬롯 0..1154에 배치**.
`build/stream1_ko_font.bin`, `build/korean_slots_full.json`, 시트 `build/korean_font_sheet.png`.

**압축 예산: 통과.** 7,841행 전부 인코딩(실패 0), 텍스트 385,926 → 542,106바이트(140.5%),
스트림 1,509,923 → 1,666,103.

| 기준 스트림 | packed | 예산 605,245 |
|---|---|---|
| 일본어 폰트 | 610,704 | **−5,459 초과** |
| 한글 폰트 | **581,909** | **+23,336 여유** |

한글 글리프가 한자보다 잘 압축돼 28,795바이트를 벌었고, 그것이 초과분을 덮었다.
**아카이브는 크기를 유지하고 ISO 재배치도 필요 없다** — `patch_iso_grow.py`는 보험으로 남는다.

측정 중 발견한 호출 오류: `encode_text(text, slots)`의 `slots`는 맵 파일 최상위가 아니라
안쪽 `"slots"` 딕셔너리다. 최상위를 넘기면 모든 조회가 빗나가 폰트가 빈 것처럼 보인다.
앞서 "슬롯 5개"로 읽은 것은 최상위 키 5개였다.

**남은 문제: 서울한강체에 없는 20자.** 廼緜龠箍真検欧玚崋宠腓斈 등 한자가 번역문에 섞여
있고 `.notdef` 박스로 그려진다. 빈 글리프 검사(0개)·타일 왕복·압축 크기 전부 통과하므로
**cmap을 봐야만 잡힌다.** 폰트 문제가 아니라 번역문 문제다.

### 패치 빌드 완료 — `build/Enkaku_Korean.iso`
`build_patch.py` → `rebuild_0000.py` → `patch_iso_inplace.py`

```
7,841행 전부 인코딩, 텍스트 385,926 → 542,096바이트
스트림 1,509,923 → 1,666,093,  참조 26,537개 재작성 (건너뜀 0)
출력 재파싱: 마커 8,822/8,822, 참조 26,537/26,537 해결, 경계 26,433 (99.61%)
아카이브 packed 583,453 / 예산 605,757 (22,304 여유), 크기 불변
ISO 739,835,904바이트 — 크기·LBA·디렉터리 레코드 전부 불변
```

**최종 검증은 ISO에서 거꾸로 읽어 수행**했다. 디렉터리 레코드로 아카이브를 찾고,
stream1을 풀고, 번역문을 인코딩해 그 바이트열이 스트림에 있는지 확인 —
**7,841/7,841행이 ISO 안에 존재**한다. 중간 파일의 자체 장부가 아니라 전 체인을 통과시킨다.

검증기를 처음엔 원본 오프셋으로 조회했다가 6개 중 1개만 맞았다. 확장 이후 전부 이동했으니
당연한 결과였고, 위치를 가정하지 않는 바이트열 검색으로 바꿔 해결했다.

### 태그 없는 포인터 배열 214개 — 매니페스트가 놓친 것
첫 빌드가 부팅 0.7초에 죽었다. 이분법으로 범인을 좁혔다.

| 빌드 | 내용 | 결과 |
|---|---|---|
| CONTROL | 압축만 재실행, 내용 동일 | **정상** → 압축기·재빌드 무죄 |
| 원본 | 미패치 | **정상** → PPSSPP 무죄 |
| STEP1 | 295행 제자리 교체, 이동 0 | **정상** → 폰트·글리프맵·인코더 무죄 |
| STEP2 | `0x120000` 이후만 확장 | **죽음** → 리플로우 확정 |

STEP2가 열쇠였다. 초반은 한 바이트도 안 움직였는데 초반에서 죽었다. 그 구간에서 바뀐 건
**초반→후반 참조 445개의 값**뿐이었고, 그중 헤더 항목의 목적지를 열어보니:

```
0x00001b: 00 80 9d 16 00 -> 0x169d80 [a4 9d 16 00 ac 9d ...]
                                      ^^^^^^^^^^^ = 0x169DA4, 또 포인터
```

`0x037040`(9개)와 `0x169A70`–`0x169DA4`(205개)가 **생 u32 포인터 배열**이었다.
대상 바이트는 `09` 105, `01` 54, `13` 42 — 태그 참조와 같은 라벨 옵코드다.

**태그 기반 수집으로는 원리적으로 찾을 수 없다.** 앞에 `01`이 없어 모양만으로는 데이터와
구별되지 않고, **헤더의 목적지를 따라가야만** 드러난다. 이것이 "다 찾았다"를 증명하려던
통계적 근거(대상 바이트 편향 35–54배)의 한계다 — 그 검정은 *찾은 것들이* 참조임은 보여도
*못 찾은 부류*가 없음은 보여주지 못한다.

`work/pointer_arrays.py` → `build/pointer_arrays.json`.
매니페스트 26,537 → **26,751개**.

### 정정: 디스패치 테이블은 존재한다 — 그리고 인터프리터를 잘못 봤다
크래시 캡처가 이 문서의 두 가지 기록을 뒤집었다.

```
Bad Execution Address
Destination: CPU Jump to 000001b4      PC: 000001b4      RA: 0884f898
```

`0x0884F86C`:
```
lw    $v0, 1160($s0)      ; 스크립트 포인터 — ctx+1160
lbu   $a0, 0($v0)         ; 옵코드 1바이트
addiu $v0, $v0, 1
lui   $v0, 0x0892
sll   $v1, $a0, 2         ; 옵코드 × 4
addiu $v0, $v0, 16856     ; 테이블 = 0x089241D8
lw    $v0, 0($v0)
jalr  $v0                 ; ← 여기서 0x1B4로 점프
```

1. **"디스패치 테이블은 없다"는 틀렸다.** `0x089241D8`에 옵코드 인덱스 테이블이 있다.
   유효 옵코드는 **0x00–0x1C (29개)**, 66개 핸들러. 0x1D 이상은 테이블 밖이라
   (그 자리엔 `"disc"` 문자열이 있다) 그대로 크래시한다.
2. **분석한 인터프리터가 틀렸다.** `0x0884A030`은 `ctx+17016`을 쓰는 **다른** 인터프리터다.
   실제 씬 스크립트를 실행하는 건 `ctx+1160`을 쓰는 이쪽이다.

**워크가 1,102,899개 명령을 어긋남 없이 걸었고 검문소 26,466개를 전부 맞혔는데도 틀렸다.**
바이트열과 일치하는 것과 그 바이트를 읽는 코드인 것은 다르다. 이 문서의 검증표들은
자기 일관성만 보증했지 올바름을 보증하지 않았다.

핸들러 대부분이 `ctx+1160`을 인라인으로 전진시키지 않는다(`0x0D`:1, `0x0F`:2, `0x10`:1,
`0x15`:1, `0x03/04`:[1,10], `0x1C`:[1,2,4]만 검출). 오퍼랜드를 헬퍼 함수로 읽을 가능성이
높으므로, 길이표는 각 핸들러를 직접 읽어 다시 만들어야 한다.

`work/dump_jumptable.py` → `build/opcode_table.json`.

### 진짜 구조: 2단계 디스패치, 자기 기술적 인자
길이는 **고정표가 아니다.** 명령은 옵코드 1바이트 + 인자 N개이고, 각 인자는 타입 태그가
자기 크기를 결정한다.

```
[1단계] 0x0884F86C — 페치 루프
   테이블 0x089241D8, 옵코드 0x00–0x1C (29개), 1바이트 전진 후 핸들러 호출

[2단계] 0x088510E0 — 인자 읽기 (핸들러가 &ctx[1160]을 넘겨 호출)
   테이블 0x0892442C, 타입 태그 1바이트 전진 후 서브핸들러 호출
```

인자 타입별 페이로드 크기 (`build/arg_table.json`):

| 태그 | 페이로드 | 의미 |
|---|---|---|
| `0x00`–`0x0E` | 0 | 태그 자체가 값 (0–14 소정수) |
| `0x10` | 4 | u32 리터럴 |
| `0x15` | 1 또는 4 | 분기 |
| `0x16` | 1 | |

이것이 스트림에서 보던 것을 정확히 설명한다:
`09 10 01 00 00 00` = 옵코드 `09` + 인자(태그 `10`, u32 `1`).
`12 10 ff ff ff ff` = 옵코드 `12` + 인자(태그 `10`, u32 `-1`).

**검출기 결함 두 개** 때문에 21개 핸들러가 "오퍼랜드 없음"으로 보였다:
1. 헬퍼는 필드의 **주소**를 받아 `0($a0)`로 쓴다 — 변위 1160으로 찾으면 안 나온다
2. 관용구가 `addiu $dst, $src, N` (dst≠src)이다 — `rs == rt`를 요구하면 전부 놓친다

### 인자 개수 (`work/arg_counts.py`)
인자를 읽는 옵코드는 5개뿐이다: `0x09`, `0x0C`, `0x0D`, `0x11`, `0x12` (각 1개).
나머지는 오퍼랜드를 인라인으로 읽는다:
`0x03/04`:[1,10], `0x0D`:1, `0x0F`:2, `0x10`:1, `0x15`:1, `0x1A`:4, `0x1C`:[1,2,4].

인자 리더는 둘인데 동작이 같다 — `0x088510E0`(jalr)과 `0x088510B4`(tail-call `jr`).
둘 다 태그 1바이트 소비 후 `0x0892442C`로 디스패치한다.

### 파서 검증 결과 (`work/parse_v2.py`)
**코드 구간에서는 맞는다.** 참조 대상 `0x03385A`에서 시작하면 9개 명령이 연속으로 유효한
옵코드(`09 13 10 0f 0d 00`)로 파싱된다. 참조 대상들이 전부 유효 옵코드로 시작한다는 것도
독립적 확인이다.

**막히는 지점: `0x1C`는 텍스트 시작 옵코드다.**
```
0x038B78:  07 1c | 81 79 88 27 ...
                   ^^^^^^^^^^^^ 여기부터 문자열, 옵코드 아님
```
`0x1C` 뒤는 종결자까지 문자열로 건너뛰어야 하는데, 인라인 전진 `[1,2,4]`만 적용하면
텍스트 두 번째 바이트를 옵코드로 읽고 멈춘다. `0x0884F260` 핸들러를 읽어 종결 조건을
확정해야 한다.

또한 스크립트 진입점은 `0x02AC80`이 아니다 — 거기는 텍스트다(`88 00`, `88 01`… 한자 코드).
`ctx+1160`이 어디서 설정되는지 찾아야 실제 시작점이 나온다.

### 길이표 (`work/lengths_v3.py` → `build/lengths_v3.json`)
`total = 1(옵코드) + inline + 인자마다 (1 태그 + 페이로드)`

| op | total | op | total | op | total |
|---|---|---|---|---|---|
| `00`–`02` | 1 | `0D` | 1 + 인자1 | `16` | 4 |
| `03`,`04` | 11 | `0E`,`0F` | 3 | `17` | 1 |
| `05`–`08` | 1 | `10` | 9 | `18` | 7 |
| `09` | 1 + 인자1 | `11`,`12` | 1 + 인자1 | `19` | 5 |
| `0A`,`0B` | 1 | `13`,`14` | 1 | `1A` | 5 |
| `0C` | 1 + 인자1 | `15` | 2 | `1B` | 3 |
| | | | | `1C` | 16 |

**검출기 결함 세 개**를 고쳐서 나온 값이다. 앞의 두 표는 전부 이 결함들 때문에 틀렸다:
1. 여러 전진을 **택일 분기**로 읽었다. 직선 코드에서는 **누적**이다 —
   `0x1C`는 `[1,2,4]`가 아니라 1+2+4+4+4 = 15다.
2. 마지막 `sw`가 **지연 슬롯**에 있는데 `jr $ra`에서 끊어 놓쳤다.
3. `addiu $sp,$sp,-16` 같은 무관한 레지스터를 추적해 음수 길이가 나왔다
   (`0x01`→−1, `0x0E`→−78). 이제 `lw $r,1160(...)`로 로드된 레지스터만 추적한다.

### 실측 결론: 대사 길이를 바꾸면 그 대사에서 죽는다
빌드 이분 탐색으로 확정했다.

| 빌드 | 확장 | 결과 |
|---|---|---|
| STEP1 | 없음 (제자리 295행) | **정상** |
| C | 끝 79행, +273B | 정상 (그 지점까지 도달 안 함) |
| E | 맨 앞 323행, +4,080B | **게임 시작 즉시 사망** |
| F | 중간 629행, +12,169B | **해당 대사에서 사망** |

E와 F가 각각 **확장한 구간에 도달하는 순간** 죽는다. 따라서 `0x148000` 이후 빌드가
"정상"이었던 것은 플레이가 거기까지 가지 않았기 때문이고, **확장은 어디서 하든 깨진다.**

배제된 원인들 (전부 실측):
- **참조 모델** — C가 26,866개를 전부 재작성하고도 정상. 여러 턴 의심했던 `01 <u32>`와
  포인터 배열은 무죄.
- **누적 크기 한계** — E는 +4KB뿐인데 죽는다.
- **길이 필드** — 대사 주변 ±16바이트에 길이와 일치하는 값 없음 (최대 1.4%, 잡음).
- **정렬** — 스팬 시작이 mod4에 25/25/25/25로 균등. 정렬 요구 없음.
- **표시 버퍼 초과** — 원본 최대 162B를 넘는 한국어는 47행(0.6%)뿐이라 E의 즉사를 설명 못 함.
- **스트림 외부 오프셋 표** — BOOT.BIN·stream0·USRDIR 13개 전부 우연 수준(1.0–1.2배).
- **헤더 `0x74`/`0x78`** — 보존해도 동일하게 사망.

남은 가설: 명령 인터프리터(`ctx+1160`)가 텍스트를 **원본 길이 기준으로 건너뛴다.**
텍스트 자체는 다른 경로(`ctx+17016`)가 읽고, 명령 쪽은 미리 계산된 양만큼 전진하는 구조라면
길이를 바꾸는 순간 desync가 나고, 크래시 지점(대사 끝 뒤의 `ff ff ff ff` 리터럴 한가운데)과
정확히 맞는다.

### 진입점: 질문 자체가 틀렸다
`work/find_entry.py`. `ctx+1160`에 쓰는 52곳 중 전진이 아닌 20곳을 추렸고, 초기화는
`0x0883D29C`다:

```
mflo  $a0
jal   0x08829CC8        ; 할당자 — 버퍼를 반환
sw    $s0, 1164($s1)
beq   $v0, $zero, ...
sw    $v0, 1160($s1)    ; ctx+1160 = 반환된 포인터
```

상위 함수를 읽으니 이 자리는 스크립트 버퍼가 **아니었다**:

```
addiu $a0, $zero, 108      ; 원소 크기 108바이트
mult  $s0, $a0             ; 개수 × 108
mflo  $a0
jal   0x08829CC8           ; alloc(개수 × 108, 1)
sw    $s0, 1164($s1)       ; +1164 = 원소 개수
sw    $v0, 1160($s1)       ; +1160 = 108바이트 구조체 배열
```

**변위 1160을 쓰는 구조체가 둘 이상이다.** 여기 베이스는 `$s1`(전역 `0x08A00000-24008`)이고,
크래시 지점의 베이스는 `$s0`(인터프리터 컨텍스트)다. `find_entry.py`가 변위만 보고 둘을
뭉뚱그렸으므로, 그 20개 후보 목록은 그대로 믿을 수 없다.

베이스 레지스터의 출처로 구분해야 한다. 크래시 프레임의 `$s0`가 어디서 오는지
(`0x0884F86C` 함수의 프롤로그와 호출자)부터 봐야 한다.

### 남은 작업: 텍스트 옵코드는 가변 길이다
표로는 아직 안 걸린다. `0x038B79`의 `1C`에서 16바이트 뒤는 `32`이고 유효 옵코드가 아니다.
텍스트는 고정 길이가 아니라 **루프로 종결자까지 읽는다** — 선형 워크로는 잡히지 않는
구조이므로 `0x0884F260`의 호출 대상이나 상위 루프를 읽어야 한다.

그리고 진입점은 여전히 미확정이다. `0x02AC80`은 텍스트다(`88 00`, `88 01`…).
`ctx+1160`이 설정되는 지점을 찾아야 한다.

순서: 텍스트 종결 조건 → 진입점 → 전체 파싱(옵코드 전부 0x00–0x1C 검증) →
참조 재수집 → 리플로우 재빌드.

검증 기준이 이번엔 외부에 있다: 파싱 중 옵코드로 읽히는 바이트가 **전부 0x00–0x1C**여야
한다. 테이블에 그 위 항목이 없어 게임이 즉시 죽기 때문이다. 이전의 자기 일관성 검증과
달리 코드가 판정한다.

### 아직 없는 것
`0x01, 0x02, 0x03, 0x07, 0x09, 0x0E, 0x17, 0x1F`의 길이. `0x0884B680`과 `0x0884E3DC`는
이 PC 필드를 쓰지 않아 기여가 없었다. 주의: `0x01`→5, `0x0E`→5는 **오염된 통계 분석에서
나온 값**이며 코드로 확인되지 않았다. 이들이 이 인터프리터 계층의 오프코드가 아닐
가능성도 있다.

### 남은 관문: 완전한 명령어 디코더
재배치의 전제는 스트림을 어긋남 없이 걸어갈 수 있는 것이다. 현재 길이표는
`00 01 02 03 0e 14 15`와 접두 조합 `09 10 / 12 10 / 13 10 / 13 12`뿐이고,
`10 ff 1c 07 40 11` 등 빈출 오프코드의 길이가 미확정이다.
디코더가 한 곳이라도 어긋나면 그 뒤 전체가 오염되므로, 이 표를 완성하는 것이
경로 B의 실질적 관문이다. BOOT.BIN의 인터프리터 루프를 MIPS로 읽는 것이 확실한 방법이다.

### 검증 계획
1. **항등 리플로우** — 증가량 0으로 리플로우했을 때 원본과 바이트 단위 일치
2. **경계 검증** — 재매핑된 모든 주소가 새 스트림에서도 명령어 경계에 착지
3. 실기 구동

## 4-1. 한글패치 관점 (갱신)

쓸 수 있는 코드 슬롯:
- 한자 블록 `0x88`~`0x8D` = **1,368 슬롯** (실사용 1,334) — 한글 음절 대부분을 여기에 배치 가능
- 히라가나 1바이트 `0x28`~`0x7A` = 83 슬롯 — 최빈 한글 음절을 넣으면 용량 절감
- 반각 카타카나 `0xA1`~`0xDF` = 63 슬롯

한국어 텍스트는 보통 1,000~1,600 음절이면 충분하므로 1,368+83+63 ≈ 1,514 슬롯으로 감당된다.
글리프는 16×16 4bpp라 한글 가독성도 확보된다.

## 4. 한글패치 관점 정리

- 사전 렌더링 UI 텍스처는 **지금 바로 교체 가능**하다. 디코더·인코더 경로가 전부 확보됨
  (LZ11 → 오프셋 테이블 → 팔레트/타일 이미지 → PNG, 및 역방향).
- 대사는 글리프 소스를 찾아야 한다. 찾으면 16×16 안티에일리어싱 글리프를 한글로 교체하는
  방식이 된다. 한글 2350자 완성형 기준 16×16 4bpp = 약 294 KB.
- 전작 방식(PGF 교체)은 적용 불가.

## 5. 도구 목록 (`work/`)

| 파일 | 용도 |
|---|---|
| `iso9660.py` | ISO9660 디렉터리 파싱 |
| `extract_iso.py` | ISO 전체 추출 |
| `lzss.py` | LZ10/LZ11 압축 해제 |
| `scan_streams.py` | 아카이브 내 LZ11 스트림 탐색 |
| `dump_archive.py` | 아카이브의 모든 스트림 덤프 |
| `texpack.py` | 컨테이너/텍스처 디코더 (A형) |
| `render_tiled.py` | 타일 이미지(B형) 조립 렌더링 |
| `survey_textures.py`, `survey_all.py`, `tile_headers.py`, `classify_streams.py` | 전수 조사 |
| `find_text.py` | 일본어 문자열 검색 (현재 히트 없음) |
| `bitmap_wall.py` | 임의 바이트 범위를 1/2/4/8bpp 비트맵으로 시각화 |
| `find_glyph_tiles.py`, `glyph_sheet.py` | PPSSPP 덤프 텍스처에서 글리프 추출 |

## 6. 2026-08-08 재점검 및 빌드 해결

이 절은 위의 초기 조사 기록과 현재 산출물의 차이를 정리한다. Claude Code 작업 중
`charmap_v2.json`은 최신인데 여러 도구가 참조하는 `charmap_final.json`이 사라져,
일본어 적용본·번역 장부·빌드 입력이 서로 다른 세대에 머물러 있었다.

### 기준 파일과 번역 장부

- `work/merge_charmap_confirmations.py`가 `charmap_v2.json`에 별도 확인 장부의
  고신뢰 문자 379건을 병합해 `charmap_final.json/.tsv`를 복원했다.
- `work/rebuild_script_ja_with_confirmations.py`를 다시 실행해 `script_full_ja.tsv`를
  9,626행으로 재생성했다.
- `work/sync_translation_ledger.py`가 텍스트 키로만 남아 있던 번역 1,785건을 행 순서
  키로 materialize했다. `work/import_translation.py`의 누락 행은 **0건**이다.
- 기존 번역문에 섞인 잘못된 일본어 문자 20종은 `fix_bad_chars.py`와
  `fix_remaining_lines.py`로 문맥에 맞게 교정했다. 최종 빌드 TSV에는 해당 문자가
  **0건** 남았다.

### 폰트·재배치·ISO 검증

- 한글 폰트: 서울한강체 B, 16×16 4bpp, **1,144자 배치 / 미지원 문자 0개 / 빈 글리프 0개**.
- 대사: **9,626/9,626행 인코딩**.
- 리플로우: 텍스트 `449,595 → 628,284`바이트, 스트림 `1,509,923 → 1,688,612`바이트,
  참조 **26,866건 재작성 / 건너뜀 0건**.
- 압축된 stream1은 `580,501`바이트로 원래 영역에 들어가므로 아카이브 확장이나 ISO
  재배치가 필요하지 않다.
- 최종 ISO `build/Enkaku_Korean.iso`는 원본과 같은 `739,835,904`바이트이며,
  `0000` 파일 영역 밖 변경 **0바이트**, 디렉터리 레코드 불변이다.
- ISO에서 역으로 읽은 결과 번역문 **9,626/9,626행**이 실제 압축 스트림에 존재한다.

### 현재 남은 실기 확인 항목

`font_extract/unresolved_kanji_audit.json`에는 낮은 신뢰도까지 포함해 113개 글리프
(실제 스크립트 419회)가 남아 있다. 이 중 `[1057]`, `[1221]`, `[185]`, `[280]`,
`[14]`, `[148]`, `[480]`은 한 글리프가 문맥별로 서로 다른 한자를 요구하는 충돌이라
전역 치환하면 오히려 원문을 훼손한다. 해당 항목은 에뮬레이터에서 화면을 확인한 뒤
문구 단위 보정으로 처리해야 한다. 이 항목들은 현재 한글 폰트 인코딩과 ISO 체인 검증을
막지는 않는다.

재현 순서는 `build/translation_ko_clean.tsv` → `build/stream1_ko_font_clean.bin` →
`build/stream1_patched_clean.bin` → `build/0000_clean.bin` →
`build/Enkaku_Korean.iso`다.
