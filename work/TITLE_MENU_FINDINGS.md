# 타이틀·시스템 메뉴 추적 기록

v2.2부터 릴리즈 노트에 이렇게 적어 왔습니다.

> 타이틀 메뉴는 찾지 못했습니다. USRDIR 전 파일(0012 404MB 포함)에 텍스처
> 스트림이 없고, 실행 파일에도 Shift-JIS/UTF-8/UTF-16 어느 형태로도 문자열이
> 없으며, 스크립트에도 없습니다.

**이 진술은 두 군데가 틀렸습니다.** 아래에 무엇이 틀렸고 무엇이 실제로 확인됐는지 적습니다.

## 1. 실행 파일에 문자열이 있습니다

없던 게 아니라 **찾는 형태가 틀렸습니다.**

이 게임은 대사를 Shift-JIS로 저장하지 않습니다. 히라가나는 0x28부터 1바이트, 한자는 0x88~0x8D 선두의 2바이트로 폰트 슬롯을 가리킵니다. 메뉴가 같은 폰트로 그려진다면 저장 방식도 같을 텐데, Shift-JIS만 훑었으니 나올 리가 없었습니다.

게임 인코딩으로 찾으니 `BOOT.BIN`에서 바로 나왔습니다. `BOOT.BIN`은 암호화되지 않은 평문 ELF입니다(엔트로피 6.39, `EBOOT.BIN`은 8.00).

```
0x103700 부근   시스템 메시지
0x103000 부근   증거품 설명
0x107c00~0x115000  인물 메모, 증언, 사건 자료
```

해독한 예:

```
セーブされていませんがよろしいですか？
メモリスティックにセーブします。
すでにインストールデータが存在します。
上書きしてよろしいですか？
メモリースティックの空き容量が不足しています。
```

## 2. 가나표에 글자가 5개 빠져 있었습니다

처음 해독하니 「メモリスティックばセーブぜゃぞ。」처럼 가나가 어긋났습니다. 스크립트 해독기의 가나표가 `あ`부터 시작하는데, **게임 표에는 앞에 작은 모음 `ぁぃぅぇぉ`가 더 있습니다.** 넣으니 「ろをぜえぬぞぐ？」가 「よろしいですか？」로 정확히 풀립니다.

## 3. 한자는 아직 못 풉니다 — 그리고 왜 못 푸는지

`88 F5 88 F6` 두 글자가 `存在`여야 합니다(「インストールデータが存在します」). 그런데 **스크립트 폰트의 245·246번 글리프는 `妻`·`赤`입니다.** 짐작이 아니라 그려서 확인했습니다. `存`은 183번, `在`는 143번에 있습니다.

같은 선두 바이트에서 trail이 1 늘었는데 슬롯이 40 뒤로 뛰므로, **계산식이 아니라 대응표**입니다. 그래서 대응표를 찾았고, 없었습니다.

| 가설 | 검증 방법 | 결과 |
|---|---|---|
| 인덱스 → Shift-JIS 표 | Shift-JIS 한자 코드가 100개 이상 연속하는 구간 검색 | 0건 |
| 인덱스 → 폰트 슬롯 표 | 245→183, 246→143, 154→170, 334→190 을 동시에 만족하는 표 검색 (u8/u16/LE/BE) | 0건 |
| 바이트쌍 → 슬롯 표 | 183·143이 인접한 위치를 찾고 나머지 세 예측으로 검증 | 0건 |
| 두 번째 글리프 아틀라스 | 4bpp 글리프 형태 점수로 전 파일 스캔 후 육안 확인 | 후보 전부 코드·데이터 |
| 폰트가 1368자보다 길다 | 원본 stream1을 글리프 1368번 이후까지 렌더링 | 잡음 (폰트는 1368에서 끝남) |

## 4. 아카이브 0001~0004에도 스트림이 있습니다

이전 스캔이 **0x800 정렬만** 봤습니다. 0000이 거기에 스트림을 두기 때문인데, 이 파일들은 **0x800 + 0x40** 위치에 둡니다. 모든 탐침이 헤더에서 64바이트씩 빗나갔고, 그래서 "텍스처 스트림 없음"이 나왔습니다.

정렬을 고치니:

```
0001    276 스트림
0002    568 스트림
0003    126 스트림
0004      9 스트림
0011      0 스트림
```

압축을 풀면 stream0과 같은 모양의 u32 오프셋 표가 나옵니다. 다만 **헤더 배치가 다릅니다.** stream0은 record 0의 첫 워드가 크기표 위치인데, 이쪽은 거기가 0이고 0x40 위치에 (표 오프셋, 개수)가 들어 있습니다. 그 값으로 읽으면 아직 말이 되는 가로·세로가 나오지 않아, 컨테이너 해독은 미완입니다.

## 5. 글꼴은 공유됩니다 — 다만 시스템 메시지만 다릅니다 (추가 조사)

"메뉴가 다른 글꼴을 쓴다"는 위의 결론은 **절반만 맞았습니다.**

스크립트가 쓰는 글꼴 슬롯으로 이름을 인코딩해 `BOOT.BIN`에서 찾아보니 **그대로 나옵니다.**

| 낱말 | 슬롯 | 바이트 | BOOT.BIN |
|---|---|---|---|
| `沼崎` | 37, 38 | `88 25 88 26` | 0x101d50 |
| `佳代` | 243, 244 | `88 f3 88 f4` | 0x102aae |
| `白川` | 21, 22 | `88 15 88 16` | 0x101ce3 |
| `斉藤` | 105, 106 | `88 69 88 6a` | 0x101e9c |

네 개가 우연히 맞을 확률은 없습니다. 그 구역(`0x101c00`~`0x102c00`)을 해독하면 `水谷`·`のぞみ`·`アリバイ`·`タクシー`·`リモコン`·`プレイヤー`·`被疑者`처럼 이 게임의 낱말이 제대로 읽힙니다. **같은 글꼴, 같은 공식입니다.**

그런데 시스템 메시지 구역(`0x1037xx`)만 어긋납니다. `セーブ` 다음에 오는 `89 48 89 49`는 슬롯 325·326인데, 그 글리프를 그려 보면 **`駅`·`便`**입니다. 화면에는 `削除`가 나옵니다. `[245][246]`도 `存在` 자리인데 글리프는 `妻`·`赤`입니다.

`0x11fd7d` 부근에는 `セーブフラグ`·`スクリプトムービー`·`ゲームオーバー時のセーブフラグ` 같은 **개발용 변수 이름**이 같은 방식으로 들어 있고, 역시 한자가 어긋납니다.

그리고 `セーブ`(바이트 `BE B0 83 75` — 반각 가나와 Shift-JIS를 섞어 씁니다)는 **ISO 전체에서 `BOOT.BIN`의 그 구역에만** 있습니다. 아카이브에도, 압축 스트림 안에도 사본이 없습니다.

즉 시스템 문자열이 가리키는 글꼴은 따로 있고, 그것은

- `USRDIR/0000` 안에 없고 (텍스처 449장 전수 확인)
- `stream1` 안의 두 번째 아틀라스도 아니며 (글리프 형태 점수로 전 구간 훑음 — 폰트는 1368자에서 끝남)
- `BOOT.BIN` 안의 4bpp 글리프도 아닙니다 (상위 후보 세 곳을 그려 보니 전부 MIPS 코드)

## 6. 역어셈블로 글꼴을 찾았습니다 (해결)

`BOOT.BIN`은 평문 MIPS ELF입니다. 파일 `0x54`가 가상 주소 `0x8804000`에 실리므로 `가상주소 = 파일오프셋 + 0x8803FAC`입니다.

**세이브 메뉴를 그리는 코드**를 먼저 찾았습니다. `0x88814cc`:

```
lw    $t0, 0x7590($v0)      ; 문자열 포인터[0] = セーブ
addiu $a0, $zero, 0xd4      ; x = 212
addiu $a1, $zero, 0x74      ; y = 116
jal   0x884cf1c             ; 그리기
...
lw    $t0, 0x7594($v0)      ; 포인터[1] = 削除
addiu $a1, $zero, 0x86      ; y = 134
```

**문자 해독기**는 `0x884be84`입니다. 첫머리가 핵심이었습니다.

```
lw    $v1, [0x8a02c14]
slt   $v1, $a0, $v1         ; 문자열이 그보다 앞이면
bnez  → $a3 = 0x88EA760     ;   실행 파일 안의 글꼴
else  → $a3 = [0x89f5e18]   ;   stream1 의 글꼴
sw    $a3, [0x8a02c38]
```

**글꼴이 둘입니다.** 대사는 stream1 것을 쓰고, 실행 파일 안의 문자열은 실행 파일 안의 것을 씁니다. "메뉴가 대사 글꼴을 이상하게 가리킨다"고 본 것이 처음부터 틀렸습니다 — 애초에 다른 글꼴이었습니다.

인코딩도 확정됐습니다.

```
1바이트 0x28..0x7A   Shift-JIS 히라가나, code = 0x8200 | (b + 0x77)
1바이트 0xA7..0xDD   반각 가타카나, 0x88EA394 의 표로 Shift-JIS 변환
2바이트 선두 0x81..0x9F, 0xE0..0xFC   code = (lead << 8) | trail
code <  0x8800       0x8890F10 글꼴 (가나·기호)
code >= 0x8800       index = ((code - 0x8800) >> 8) * 253 + (code & 0xFF)
                     glyph = base + (index >> 1) * 256
```

즉 **가나는 평범한 Shift-JIS**이고, 한자는 Shift-JIS가 비워 둔 `0x8800`부터를 사유 영역으로 쓴 것입니다.

`253`이 상수 검색에 안 걸린 이유도 밝혀졌습니다. 컴파일러가 `((x << 6) - x) << 2) + x`로 풀어 놓았습니다(`0x884c03c`).

**검증**했습니다. 이 글꼴에서

```
325·326 = 削除    245·246 = 存在    154·334 = 上書    0·1 = 黙秘
```

예측이 전부 맞습니다. 문자열도 온전히 읽힙니다 — `削除`, `黙秘：`, `人物：`, `場所：`, `証拠：`, `１１月`, `の調査予定`, `【調査場所】`, `を入手`, `午前と午後の予定は`, `調査を終了しますか？`.

글꼴은 파일 `0xe67b4`부터 세그먼트 끝까지, **1952 글리프**입니다. 완전히 빈 타일은 **2개(4 글리프)**뿐입니다.

**타이틀 메뉴 문자열은 실행 파일에 없습니다.** `から`(바이트 `32 70`) 71곳을 전부 해독했지만 `最初から`·`続きから`는 없습니다. 그것은 아직 다른 곳입니다.

## 남은 장벽: 실행 파일은 암호화되어 배포됩니다

`BOOT.BIN`은 평문이지만 **PSP가 실제로 싣는 것은 `EBOOT.BIN`**이고, 그쪽은 `~PSP` 암호화본입니다(엔트로피 8.00). `BOOT.BIN`을 고쳐도 게임에 반영되지 않습니다.

한글로 바꾸려면 실행 파일을 고쳐야 하고, 그러려면

- `EBOOT.BIN` 자리에 평문 ELF를 넣어야 합니다 (CFW·PPSSPP는 받아들이지만 정품 펌웨어는 아닙니다), 그리고
- 글리프 자리가 4개뿐이므로, 메뉴가 쓰지 않는 한자 슬롯을 찾아 한글로 덮어써야 하며,
- 문자열은 제자리에서 길이를 늘릴 수 없습니다. `セーブ`(5바이트)를 `저장`(4+NUL=5)으로, `削除`(5바이트)를 `삭제`(5)로 바꾸는 것은 정확히 들어맞습니다.

이 프로젝트는 실기 구동을 지키려고 ISO를 제자리에서만 고쳐 왔습니다(v1.1의 `C1-2858-3`). 실행 파일 교체는 그 조건을 깨므로, 진행 여부는 판단이 필요합니다.

## 관련 도구

`mipsdis.py`(VFPU를 건너뛰며 이어 붙이는 디스어셈블러), `mips_dump.py`, `mips_global_xref.py`, `find_refs.py`, `find_glyph_code.py`, `show_boot_font.py`, `decode_menu.py`.

## 다음에 볼 곳

1. **0001~0004 컨테이너 헤더 확정.** 스트림은 열리고 팔레트(1024바이트)도 보이므로 남은 것은 크기표 배치뿐입니다. 가장 유망합니다.
2. **BOOT.BIN 역어셈블.** 메뉴를 그리는 함수가 글리프를 어디서 읽는지 따라가면 한자 출처가 나옵니다. 확실하지만 비쌉니다.
3. 한글 패치는 **한자 출처를 찾은 뒤**에야 가능합니다. 한글 글리프는 stream1 폰트에 넣어 두었는데, 메뉴가 그 폰트를 쓰지 않는다면 닿지 않습니다.

## 관련 도구

| 파일 | 역할 |
|---|---|
| `find_title.py` | 파일별 정체·엔트로피 판별 후 Shift-JIS 계열 검색 |
| `find_title_encoded.py` | 게임 인코딩으로 변환해 검색 (여기서 발견) |
| `decode_boot_text.py` | BOOT.BIN 구간을 게임 인코딩으로 해독 |
| `sweep_boot_text.py` | BOOT.BIN 전체에서 텍스트 구간 탐색 |
| `find_kanji_table.py` `find_remap.py` `find_pair_table.py` | 대응표 가설 세 가지 검증 (모두 0건) |
| `find_menu_font.py` `show_raw_glyphs.py` `show_glyph_range.py` | 두 번째 아틀라스 탐색과 육안 확인 |
| `scan_archives.py` | LZ11 스트림 스캔 (정렬 지정 가능) |
| `dump_archive_textures.py` | 0001~0003 컨테이너 해독 시도 (미완) |

---

## 7. 타이틀 화면 문구는 `最初から`가 아니었습니다

이 문서가 지금까지 찾던 대상 자체가 틀렸습니다. 제보 스크린샷의 타이틀 화면에는 메뉴 목록이 없습니다.

```
遠隔捜査 (로고)
真実への23日間 (부제)
STARTボタンを押してください      <- 이것뿐입니다
(C) 2009 Sony Computer Entertainment Inc.
```

`最初から`·`続きから`는 **이 게임에 존재하지 않습니다.** `から` 71곳을 전부 해독하고도 못 찾은 이유가 이것입니다 — 없는 것을 찾고 있었습니다.

## 8. 그 문구는 그림입니다

`STARTボタンを押してください`의 히라가나 꼬리 `してください`(게임 인코딩 `3e 4d 36 47 3c 2b`)로 디스크 전체를 훑었습니다. `hunt_title.py`가 원본 파일 열 개와 그 안의 LZ11 스트림들, 모두 187개 덩어리를 봅니다.

| 찾은 곳 | 결과 |
|---|---|
| `BOOT.BIN` | 없음 |
| `0000` stream0 (UI 텍스처) | 없음 |
| `0000` stream1 (스크립트) | 13곳, **전부 평범한 대사** (`【…】`가 앞에 붙어 있음) |
| 나머지 아카이브 | 압축 데이터 안의 우연한 일치뿐 |

평문 Shift-JIS도 확인했습니다 — `最初から`·`続きから`·`この物語`·`フィクション`·`ください`·`データインストール` 여섯 가지 모두 **디스크 어디에도 없습니다.** 이 게임에 평문 Shift-JIS 문자열은 존재하지 않습니다.

글자꼴도 말해 줍니다. 부제와 같은 명조체에 `START`만 빨간색이고 자간이 고르게 벌어져 있습니다. 폰트로 그린 것이 아니라 **미리 그려 둔 이미지**입니다.

`0000`의 텍스처 449장을 릴리즈본 상태로 전부 그려서 확인했습니다(`survey_untouched.py`). 타이틀 문구는 그중에 없습니다. 한 번도 손대지 않은 191장도 초상화·이펙트·`Congratulations!`·`NOW LOADING`·화살표뿐이고 일본어 문구는 없습니다.

**따라서 타이틀 자산은 `0001~0004` 안에 있습니다.**

## 9. 0001~0004 컨테이너 — 절반은 풀렸습니다

`0000`과 달리 이 아카이브들은 **레코드마다 자기 헤더를 답니다.** `0000` 방식으로 읽었을 때 1,056바이트짜리 레코드에서 "512x256 tile 32x32 psm 5"가 나왔던 것이 그 증거입니다.

```
u16 width, u16 height, u16 tile_w, u16 tile_h, u16 psm, u16 flag, u32 pixel_start
```

파일 머리도 다릅니다.

```
0002 0003 0004   0x00 에 MD5 16바이트, 0x40 부터 LZ11
0001 0011        0x00 에 61 0d 0a 00, 0x840 부터 LZ11
```

`decode_container.py`로 아홉 장이 나옵니다 — 512x256 T8, 팔레트는 RGBA8888 256색. 실내·거리 배경으로 알아볼 수 있게 나오지만 **타일 배치가 아직 안 맞습니다.** 타일 순서(행 우선·열 우선)와 타일 안의 배치(16x8 스위즐·선형·전치) 여섯 조합을 모두 시도했고 전부 찢어집니다. 픽셀 앞 `0x78`바이트 헤더 안에 타일 지도가 있을 가능성이 남아 있습니다.

그리고 이 아홉 장은 아카이브의 일부일 뿐입니다. LZ11이 차지하는 비율은 `0001` 19%, `0002` 3%, `0003` 72%, `0004` 6%입니다. 나머지는 압축되지 않은 채 들어 있고, 그것을 훑을 목록을 아직 찾지 못했습니다.

## 10. 다음에 볼 곳 (갱신)

1. **`0001~0004`의 파일 목록.** LZ11이 아닌 나머지를 열 방법이 없으면 타이틀 자산에 닿을 수 없습니다. 0x40 블록 헤더가 반복되는지부터 확인해야 합니다.
2. **타일 지도.** 픽셀 시작 전 `0x78`바이트를 해독하면 아홉 장이 온전해집니다.
3. **타이틀을 그리는 코드.** 글자를 그리는 `0x884cf1c`가 아니라 텍스처를 올리는 쪽을 따라가면 어느 아카이브의 몇 번째 자산인지 나옵니다.

---

## 11. 아카이브가 열렸습니다

두 가지가 막고 있었고 둘 다 풀렸습니다.

### 블록 목록

LZ11 을 바이트 스캔으로 찾은 것이 문제였습니다. 스트림 하나를 풀고 압축 길이만큼 건너뛰면 **패딩 한가운데 떨어져** 나머지를 잃습니다. 실제 구조는 `0x800` 정렬 블록의 연속이고, 블록마다 `0x40` 헤더(MD5 16바이트 + 제로)가 붙습니다.

```
0002 0003 0004   0x00 에 MD5, 0x40 부터 LZ11
0001 0011        0x00 에 61 0d 0a 00, 0x840 부터
```

| | 스캔 | 블록 목록 |
|---|---|---|
| 0001 | 19% | 329 스트림 |
| 0002 | 3% | 566 스트림 |
| 0003 | 72% | 126 스트림 |
| 0004 | 6% | 9 스트림 |

### 타일 컨테이너

픽셀이 연속된 런이 아닙니다. 오프셋 16 부터 타일 배열입니다.

```
u16 tile_x, u16 tile_y, 12바이트, 그리고 tile_w x tile_h, 16x8 스위즐
```

타일 앞 16바이트를 픽셀로 그리고 있었으니 매 타일마다 어긋났습니다. 그리고 오프셋 12 의 워드는 시작 위치가 아니라 **타일 개수**입니다.

```
배너     16 + 16 x 1040 = 16,656 = 레코드 길이
배경     16 + 120 x 1040 = 124,816
512x512  16 + 240 x 1040 = 249,616
```

타일마다 자기 좌표를 들고 있어서 빈 타일을 빼도 제자리에 놓입니다. 512x256 이 128 타일이 아니라 120 타일만 저장하는 이유입니다.

**784장이 온전히 나옵니다.**

## 12. 그 안에서 나온 것

한 번도 손대지 못한 일본어가 있습니다.

| | 수량 | 예 |
|---|---|---|
| 튜토리얼 페이지 | 24 | `調査依頼について` 4쪽, `辞典について` 3쪽, `尋問について`, `仲間について`, `捜査資料について` |
| 장소 이름표 | 30 | `社長室`, `事務所`, `職員…`, `トイレ` |
| 정보 라벨 | 15 | |
| 장면 배너 | 12 | `尋問開始`, `尋問終了`, `前日の調査報告`, `報告終了` |
| 시작 안내 | 1 | CERO 및 체험판 주의 문구 |

`build/container_text` 에 뽑아 두었습니다(`dump_container_text.py`).

## 13. 0000 에도 빠진 것이 있었습니다

텍스처 449장을 원본과 릴리즈본을 나란히 놓고 다시 보니, **이름표 여섯 장이 아직 일본어**입니다. 339 `吉本ユミ` 는 `요시모토 유미` 로 바뀌었는데 다음은 그대로입니다.

```
340 近藤克美   350 沼崎栄太郎   352 斉藤光志
353 白川のぞみ 355 沼崎晋太郎   360 水無月幸司
```

증거 카드 54 `黙秘` 도 손대지 않았습니다.

## 14. 타이틀 문구 — 남은 곳은 영상뿐입니다

디스크 전체가 설명됐습니다.

| 파일 | 정체 |
|---|---|
| 0000 | UI 텍스처 449장 + 스크립트 |
| 0001~0004 | 배경·인물·튜토리얼·배너 784장 |
| 0010 0011 0013 | `SGXD` 음성 |
| 0012 | `VSTD` 영상 블록 23,902개 |
| 0020~0023 | `PSMF` 동영상 4편 |
| BOOT.BIN / EBOOT.BIN | 실행 파일 |

`STARTボタンを押してください` 는 이 중 어디에도 문자열로도 그림으로도 없습니다. 시도한 형태는 게임 인코딩(1바이트 가나), 평문 Shift-JIS, Shift-JIS 리틀엔디안 u16, 같은 것을 u32 로 채운 것, ASCII `START`, UTF-16 `START` 입니다. 전부 0건입니다.

타이틀 화면의 배경은 찾았습니다 — **0000 의 텍스처 433**(신문 위 남자 옆얼굴)과 **434**(여자 옆얼굴)입니다. 로고와 문구는 그 텍스처에 들어 있지 않습니다.

남은 후보는 **`PSMF` 동영상**입니다. 타이틀 화면이 영상 루프이고 문구가 프레임에 새겨져 있다면 지금까지의 결과가 전부 설명됩니다. 확인하려면 PSMF 디먹서가 필요합니다.
